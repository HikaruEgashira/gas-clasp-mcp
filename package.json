{
  "name": "gas-clasp-mcp",
  "version": "0.1.0",
  "description": "Model Context Protocol (MCP) server for Google Clasp integration",
  "main": "mcp.ts",
  "bin": {
    "gas-clasp-mcp": "mcp.ts"
  },
  "scripts": {
    "start": "tsx mcp.ts",
    "lint": "biome lint . && biome format --write ."
  },
  "keywords": ["mcp", "clasp", "google-apps-script"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.22.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.17.30",
    "tsx": "^4.19.3",
    "typescript": "^5.3.2"
  }
}
